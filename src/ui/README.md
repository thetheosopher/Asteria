# UI Placeholder

This folder is reserved for the future native desktop UI shell.

Recommended direction:
- Qt 6 Widgets for the early native Windows desktop shell
- Main window + Library workspace + Chart workspace + Compare workspace + Settings

The current bootstrap build intentionally avoids a hard Qt dependency so you can start iterating on domain, engine, and rendering immediately.
